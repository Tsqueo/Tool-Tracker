rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Initial 4-person deployment: Firebase Authentication is required for all app data.
    // V3's Admin/Staff/private controls are also enforced in the application UI.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
